services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: express-api
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
    restart: unless-stopped
